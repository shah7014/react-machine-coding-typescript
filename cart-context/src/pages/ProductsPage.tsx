import ProductsList from "../features/products/ProductsList";

const ProductsPage = () => {
  return (
    <div className="page">
      <ProductsList />
    </div>
  );
};

export default ProductsPage;
